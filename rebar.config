%% Dependencies
{deps, [{lfe, "0.6.*", {git, "https://github.com/rvirding/lfe.git", "HEAD"}}]}.

%% LFE files to compile before the rest
{lfe_first_files, []}.

%% Options for the LFE compiler
{lfe_opts, []}.
